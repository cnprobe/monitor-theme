# Optional bridge credentials. Keep real values in a local .env or secret manager.
# The built-in Komari adapter uses the panel's public guest APIs; do not put a
# Komari admin key here. These variables are for adapters that support tokens.
SERVER_STATUS_TOKEN=
NEZHA_TOKEN=
